version: 2.1
jobs:
  build:
    docker:
      - image: cimg/php:7.4
    steps:
      - run: echo "Actual build is in gitlab; this is just kept here so builds don't fail until CircleCI is removed for the project."
